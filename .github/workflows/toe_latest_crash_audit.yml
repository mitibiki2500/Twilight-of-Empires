name: ToE Latest Crash Audit
on:
  push:
    branches: [ main ]
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run latest crash audit
        run: python scripts/toe_latest_crash_audit.py
