# Disabled by ToE performance/stability cleanup (2026-08-28).
# The bundled GDPGR package is incomplete: GDPGR_initialize_all / GDPGR_initialize_country
# and the variables maintained by the original package are absent.
# Keeping the old AI-only decision caused every AI country to repeatedly evaluate an
# any_country scan for an initialization path that can never complete.
# GDPGR runtime initialization is intentionally disabled together with GDPGR_on_actions.txt.
