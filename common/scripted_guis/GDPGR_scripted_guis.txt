# Disabled by ToE performance/stability cleanup (2026-08-28).
# The GDPGR data-producing runtime is not present in this mod, so these leaderboard
# scripted GUIs only reference variables that are never populated. Keep them disabled
# unless the complete GDPGR system is restored.
