# Twilight of Empires total-conversion compatibility
# Vanilla Canada historical character templates are intentionally disabled.
# ToE reuses the CAN country tag for a non-vanilla country, so vanilla Canada-specific
# character template conditions can evaluate against the wrong country/state context.
# This file shadows the vanilla file at the same path; generic character generation is unaffected.
