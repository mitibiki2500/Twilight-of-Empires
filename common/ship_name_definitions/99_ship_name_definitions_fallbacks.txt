# ToE fallback ship-name property template.
# Vanilla country-specific prefix triggers are intentionally removed because ToE replaces the country database.
shipname_prefixes_clone_template = {
	template = yes
	properties = { }
}
